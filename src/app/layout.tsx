import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CAD Viewer - Professional 3D CAD Review Platform",
  description: "Industry-grade CAD collaboration platform for reviewing 3D models, annotating designs, and team collaboration.",
  keywords: ["CAD", "3D viewer", "engineering", "collaboration", "review"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${inter.variable} font-sans antialiased`}
        suppressHydrationWarning
      >
        {children}
      </body>
    </html>
  );
}
